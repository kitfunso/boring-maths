export { default as MacroCalculator } from './MacroCalculator';
export { calculateMacros, formatNumber } from './calculations';
export { getDefaultInputs, activityLabels, goalLabels } from './types';
export type {
  MacroCalculatorInputs,
  MacroCalculatorResult,
  Gender,
  ActivityLevel,
  Goal,
  UnitSystem,
} from './types';
